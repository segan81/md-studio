export interface RenderOptions {
  sanitize?: boolean;
}

export class MarkdownEngine {
  render(markdown: string, _options?: RenderOptions): string {
    // Implementación temporal
    return markdown;
  }
}