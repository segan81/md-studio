{
  "name": "md-studio",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.12.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter chrome-extension dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  }
}